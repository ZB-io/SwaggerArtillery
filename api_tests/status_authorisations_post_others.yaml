# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /status/authorisations_post for http method type POST in artillery framework
# RoostTestHash=d7fad32477
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "status_authorisations_post_others.csv"
    fields:
      - "merchantRequestIdentification"
      - "creditorAccount.bankCode"
      - "creditorAccount.accountIdentification"
      - "creditorAccount.currency"
      - "creditorAccount.accountSchemeName"
      - "creditorAccount.accountSubType"
    skipHeader: true
    delimiter: "^|^"
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    x-fapi-customer-ip-address: "123.123.123.123"
    x-fapi-auth-date: "Tue, 15 Nov 1994 08:12:31 GMT"
    x-fapi-interaction-id: "urn:uuid:3d5ea426-f2c6-11eb-bea5-0242ac130002"
    x-idempotency-key: "3d5ea426-f2c6-11eb-bea5-0242ac130002"
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - post:
          url: "/status/authorisations"
          json:
            merchantRequestIdentification: "{{ merchantRequestIdentification }}"
            creditorAccount:
              bankCode: "{{ creditorAccount.bankCode }}"
              accountIdentification: "{{ creditorAccount.accountIdentification }}"
              currency: "{{ creditorAccount.currency }}"
              accountSchemeName: "{{ creditorAccount.accountSchemeName }}"
              accountSubType: "{{ creditorAccount.accountSubType }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "error_description"
      - think: 1
      - post:
          url: "/invalid/url"
          expect:
            - statusCode: 404
      - think: 1
