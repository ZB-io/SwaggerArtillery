# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /status/instructions_post for http method type POST in artillery framework
# RoostTestHash=29c28aa003
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p99: 200
  plugins:
    expect: {}
  payload:
    path: "status_instructions_post_others.csv"
    fields: ["merchantInstructionIdentification", "creditorAccount.bankCode", "creditorAccount.accountIdentification", "creditorAccount.currency", "creditorAccount.accountSchemeName", "creditorAccount.accountSubType"]
    skipHeader: true
    delimiter: "^|^"
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    x-fapi-customer-ip-address: "192.168.1.1"
    x-fapi-auth-date: "2022-01-01"
    x-fapi-interaction-id: "123456"
    x-idempotency-key: "abcdefg"

scenarios:
  - name: Non-Success API Response Test
    flow:
      - log: "Sending invalid POST request to /status/instructions"
      - post:
          url: "/status/instructions?merchantInstructionIdentification={{ merchantInstructionIdentification }}&creditorAccount.bankCode={{ creditorAccount.bankCode }}&creditorAccount.accountIdentification={{ creditorAccount.accountIdentification }}&creditorAccount.currency={{ creditorAccount.currency }}&creditorAccount.accountSchemeName={{ creditorAccount.accountSchemeName }}&creditorAccount.accountSubType={{ creditorAccount.accountSubType }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending POST request to /status/instructions with missing headers"
      - post:
          url: "/status/instructions?merchantInstructionIdentification={{ merchantInstructionIdentification }}&creditorAccount.bankCode={{ creditorAccount.bankCode }}&creditorAccount.accountIdentification={{ creditorAccount.accountIdentification }}&creditorAccount.currency={{ creditorAccount.currency }}&creditorAccount.accountSchemeName={{ creditorAccount.accountSchemeName }}&creditorAccount.accountSubType={{ creditorAccount.accountSubType }}"
          headers:
            x-fapi-customer-ip-address: ""
            x-fapi-auth-date: ""
            x-fapi-interaction-id: ""
            x-idempotency-key: ""
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending POST request to invalid URL"
      - post:
          url: "/invalid/url?merchantInstructionIdentification={{ merchantInstructionIdentification }}&creditorAccount.bankCode={{ creditorAccount.bankCode }}&creditorAccount.accountIdentification={{ creditorAccount.accountIdentification }}&creditorAccount.currency={{ creditorAccount.currency }}&creditorAccount.accountSchemeName={{ creditorAccount.accountSchemeName }}&creditorAccount.accountSubType={{ creditorAccount.accountSubType }}"
          expect:
            - statusCode: 404
      - think: 1
