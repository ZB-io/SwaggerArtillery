# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /direct-debit-consents_post for http method type POST in artillery framework
# RoostTestHash=e1f4067438
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  authToken: "{{ $processEnvironment.AUTH_TOKEN }}"
  plugins:
    expect: {}
  payload:
    path: "direct-debit-consents_post_success.csv"
    fields:
      - "permissions"
      - "expirationDate"
    skipHeader: true
    delimiter: "^|^"
  environments:
    variable:
      target: "{{ config.target }}"

scenarios:
  - name: "Test successful API responses"
    flow:
      - log: "Starting test scenario for successful API responses"
      - post:
          url: "/direct-debit-consents"
          headers:
            Authorization: "Bearer {{ config.authToken }}"
            Content-Type: "application/json"
          json:
            data:
              permissions: "{{ permissions }}"
              expirationDate: "{{ expirationDate }}"
          expect:
            - statusCode: 201
            - hasProperty: "data.consentId"
            - hasProperty: "data.creationDate"
            - hasProperty: "data.status"
            - hasProperty: "data.statusUpdateDate"
            - hasProperty: "data.permissions"
            - hasProperty: "data.expirationDate"
            - hasProperty: "links.self"
            - hasProperty: "links.prev"
            - hasProperty: "links.next"
            - hasHeader: "x-fapi-interaction-id"
      - think: 1
