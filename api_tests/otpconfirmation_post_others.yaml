# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /otpconfirmation_post for http method type POST in artillery framework
# RoostTestHash=3bab7c72fd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "otpconfirmation_post_others.csv"
    fields: ["mandateIdentification", "otpIdentificationNumber", "otpPassword", "bankCode", "accountIdentification", "currency", "accountSchemeName", "accountSubType"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending invalid POST request to /otpconfirmation"
      - post:
          url: "/wrongendpointhere"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-customer-ip-address: "192.168.1.1"
            x-fapi-auth-date: "2021-06-01T09:00:00Z"
            x-fapi-interaction-id: "string"
            x-idempotency-key: "string"
          json:
            mandateIdentification: "{{ mandateIdentification }}"
            otpIdentificationNumber: "{{ otpIdentificationNumber }}"
            otpPassword: "{{ otpPassword }}"
            creditorAccount:
              bankCode: "{{ bankCode }}"
              accountIdentification: "{{ accountIdentification }}"
              currency: "{{ currency }}"
              accountSchemeName: "{{ accountSchemeName }}"
              accountSubType: "{{ accountSubType }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending POST request to /otpconfirmation without auth token"
      - post:
          url: "/otpconfirmation"
          headers:
            x-fapi-customer-ip-address: "192.168.1.1"
            x-fapi-auth-date: "2021-06-01T09:00:00Z"
            x-fapi-interaction-id: "string"
            x-idempotency-key: "string"
          json:
            mandateIdentification: "{{ mandateIdentification }}"
            otpIdentificationNumber: "{{ otpIdentificationNumber }}"
            otpPassword: "{{ otpPassword }}"
            creditorAccount:
              bankCode: "{{ bankCode }}"
              accountIdentification: "{{ accountIdentification }}"
              currency: "{{ currency }}"
              accountSchemeName: "{{ accountSchemeName }}"
              accountSubType: "{{ accountSubType }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending POST request to /otpconfirmation with invalid payload"
      - post:
          url: "/otpconfirmation"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-customer-ip-address: "192.168.1.1"
            x-fapi-auth-date: "2021-06-01T09:00:00Z"
            x-fapi-interaction-id: "string"
            x-idempotency-key: "string"
          json:
            mandateIdentification: 12345678
            otpIdentificationNumber: 12345678
            otpPassword: 12345678
            creditorAccount:
              bankCode: 12345678
              accountIdentification: 12345678
              currency: 12345678
              accountSchemeName: 12345678
              accountSubType: 12345678
          expect:
            - statusCode: 400
      - think: 1
