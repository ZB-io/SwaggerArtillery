# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=ae50169768
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test various non-successful API requests"
    flow:
      - log: "Testing with incorrect URL"
      - get:
          url: "/v2/pets/{{ petId }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing with missing petId"
      - delete:
          url: "/v2/pet/"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing with incorrect petId"
      - delete:
          url: "/v2/pet/abc"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing with missing authorization"
      - delete:
          url: "/v2/pet/{{ petId }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing with incorrect authorization"
      - delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "IncorrectToken"
          expect:
            - statusCode: 403
      - think: 1
