# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByTags_get for http method type GET in artillery framework
# RoostTestHash=c791547528
# 
# 

# ********RoostGPT********
config:
  target: "http://your-target-url.com"
  plugins:
    expect: {}
  environments:
    dev:
      target: "http://your-dev-target-url.com"
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
        
scenarios:
  - name: "Non-success API responses test"
    flow:
      # Testing GET request with missing authorization token
      - get:
          url: "/v2/pet/findByTags?tags={{ tags }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
        think: 1

      # Testing GET request with invalid URL
      - get:
          url: "/v2/pet/invalidURL?tags={{ tags }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Testing GET request with invalid parameters
      - get:
          url: "/v2/pet/findByTags?invalidParam={{ tags }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1
