# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_post for http method type POST in artillery framework
# RoostTestHash=99d9f02040
# 
# 

# ********RoostGPT********
config:
  target: "your_api_host"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_post_success.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true

scenarios:
  - name: "API Test Scenario"
    flow:
      # Updating the pet in the store with form data
      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "your_auth_token"
          json:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "name"
            - hasProperty: "status"
      - think: 1
