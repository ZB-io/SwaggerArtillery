# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByStatus_get for http method type GET in artillery framework
# RoostTestHash=eb30f4afde
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_findByStatus_get_success.csv"
    fields:
      - "status"
    skipHeader: true
  headers:
    api_key: "{{$processEnvironment.AUTH_TOKEN}}"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Petstore API test"
    flow:
      # API request to find pets by status
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
      - think: 1
