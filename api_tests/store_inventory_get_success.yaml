# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/inventory_get for http method type GET in artillery framework
# RoostTestHash=a66f7079af
# 
# 

# ********RoostGPT********
config:
  target: "{{ process.env.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "store_inventory_get_success.csv"
    fields:
      - "productId"
      - "quantity"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Store inventory status check"
    flow:
      # Get request to fetch inventory status
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "{{ process.env.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "productId"
            - hasProperty: "quantity"
      - think: 1
