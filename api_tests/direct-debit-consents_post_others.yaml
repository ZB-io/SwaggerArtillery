# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /direct-debit-consents_post for http method type POST in artillery framework
# RoostTestHash=e1f4067438
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "direct-debit-consents_post_others.csv"
    fields:
      - "permissions"
      - "expirationDate"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Sending invalid POST request to /direct-debit-consents"
      - post:
          url: "/direct-debit-consents"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
            x-fapi-auth-date: "2022-10-10T10:10:10Z"
            x-fapi-customer-ip-address: "127.0.0.1"
            x-fapi-interaction-id: "abcd1234"
            Accept-Language: "en-US"
          json:
            data:
              permissions: "{{ permissions }}"
              expirationDate: "{{ expirationDate }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 1
      - log: "Sending POST request to incorrect URL"
      - post:
          url: "/direct-debit-consents-invalid"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
            x-fapi-auth-date: "2022-10-10T10:10:10Z"
            x-fapi-customer-ip-address: "127.0.0.1"
            x-fapi-interaction-id: "abcd1234"
            Accept-Language: "en-US"
          json:
            data:
              permissions: "{{ permissions }}"
              expirationDate: "{{ expirationDate }}"
          expect:
            - statusCode: 404
      - think: 1
