# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /direct-debit-consents_post for http method type POST in artillery framework
# RoostTestHash=93cc6baa2f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "direct-debit-consents_post_others.csv"
    fields:
      - "permissions"
      - "expirationDate"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Sending a POST request with invalid payload"
      - post:
          url: "/direct-debit-consents"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            data:
              permissions: "{{ permissions }}"
              expirationDate: "{{ expirationDate }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
            - hasProperty: "errors[0].code"
            - equals: ["errors[0].message", "Invalid payload"]
      - think: 1

      - log: "Sending a POST request without authorization token"
      - post:
          url: "/direct-debit-consents"
          headers:
            Content-Type: "application/json"
          json:
            data:
              permissions: "{{ permissions }}"
              expirationDate: "{{ expirationDate }}"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
            - hasProperty: "errors[0].code"
            - equals: ["errors[0].message", "Authorization token is missing"]
      - think: 1

      - log: "Sending a POST request to an incorrect URL"
      - post:
          url: "/incorrect-url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            data:
              permissions: "{{ permissions }}"
              expirationDate: "{{ expirationDate }}"
          expect:
            - statusCode: 404
            - hasProperty: "errors"
            - hasProperty: "errors[0].code"
            - equals: ["errors[0].message", "URL not found"]
      - think: 1
