# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user_post for http method type POST in artillery framework
# RoostTestHash=fcaadbca4d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_post_others.csv"
    fields:
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
  http:
    pool: 10

scenarios:
  - name: "Non-success API responses"
    flow:
      - post: # Testing POST with invalid payload
          url: "/v2/user"
          headers:
            Content-Type: "application/json"
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 400
      - think: 1

      - post: # Test POST with invalid endpoint
          url: "/v2/invalid"
          headers:
            Content-Type: "application/json"
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 404
      - think: 1

      - get: # Test GET with missing authorization
          url: "/v2/user/{{ username }}"
          expect:
            - statusCode: 401
      - think: 1

      - get: # Test GET with invalid authorization
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "invalid_token"
          expect:
            - statusCode: 403
      - think: 1
