# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /reports_post for http method type POST in artillery framework
# RoostTestHash=73c5a4b1b6
# 
# 

# ********RoostGPT********
config:
  target: "http://example.com"  # Replace this with your actual target URL
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "reports_post_success.csv"
    fields:
      - "reportType"
      - "reportingDate"
      - "creditorAccountBankCode"
      - "creditorAccountAccountIdentification"
      - "creditorAccountCurrency"
      - "creditorAccountAccountSchemeName"
      - "creditorAccountAccountSubType"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "POST /reports success scenario"
    flow:
      - log: "Starting POST /reports request"
      - post:
          url: "/reports"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"  # Ensure this environment variable is set in your runtime environment
            x-fapi-customer-ip-address: "192.168.1.1"
            x-fapi-auth-date: "2022-01-01T00:00:00Z"
            x-fapi-interaction-id: "123456789"
            x-idempotency-key: "123456789"
          json:
            reportType: "{{ reportType }}"
            reportingDate: "{{ reportingDate }}"
            creditorAccount:
              bankCode: "{{ creditorAccountBankCode }}"
              accountIdentification: "{{ creditorAccountAccountIdentification }}"
              currency: "{{ creditorAccountCurrency }}"
              accountSchemeName: "{{ creditorAccountAccountSchemeName }}"
              accountSubType: "{{ creditorAccountAccountSubType }}"
          expect:
            - statusCode: 201
            - hasProperty: "processResult"
            - hasProperty: "processResult.responseCode"
            - hasProperty: "processResult.rejectReasonList"
            - hasProperty: "reportContent"
      - think: 1
