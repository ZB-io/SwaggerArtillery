# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /reports_post for http method type POST in artillery framework
# RoostTestHash=73c5a4b1b6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "reports_post_success.csv"
    fields:
      - "reportType"
      - "reportingDate"
      - "creditorAccount.bankCode"
      - "creditorAccount.accountIdentification"
      - "creditorAccount.currency"
      - "creditorAccount.accountSchemeName"
      - "creditorAccount.accountSubType"
    skipHeader: true
    delimiter: "^|^"
  
scenarios:
  - name: "POST /reports success scenario"
    flow:
      - log: "Starting POST /reports request"
      - post:
          url: "/reports"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-customer-ip-address: "127.0.0.1"
            x-fapi-auth-date: "2022-01-01T00:00:00Z"
            x-fapi-interaction-id: "efa78fda-e89a-4cbb-aca5-2b6e4a9f9e9d"
            x-idempotency-key: "efa78fda-e89a-4cbb-aca5-2b6e4a9f9e9d"
          json:
            reportType: "{{ reportType }}"
            reportingDate: "{{ reportingDate }}"
            creditorAccount:
              bankCode: "{{ creditorAccount.bankCode }}"
              accountIdentification: "{{ creditorAccount.accountIdentification }}"
              currency: "{{ creditorAccount.currency }}"
              accountSchemeName: "{{ creditorAccount.accountSchemeName }}"
              accountSubType: "{{ creditorAccount.accountSubType }}"
      - expect: 
          - statusCode: 201
      - expect: 
          - hasProperty: "processResult"
      - expect: 
          - hasProperty: "processResult.responseCode"
      - expect: 
          - hasProperty: "processResult.rejectReasonList"
      - expect: 
          - hasProperty: "reportContent"
      - think: 1
