# ********RoostGPT********

# Test generated by RoostGPT for test raml-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/ramls_get for http method type GET in artillery framework
# RoostTestHash=347ea070d6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "__ramls_get_success.csv"
    fields: ["auth"]
    skipHeader: true
    delimiter: "^|^"
  variables:
    auth: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: Validate successful API responses
    flow:
      - log: "Starting test for /_/ramls endpoint"
      - get:
          url: "/_/ramls"
          headers:
            X-Okapi-Module-Id: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "application/json"
            - hasProperty: "application/raml+yaml"
            - equals:
                "application/json": ["proxiesFor.raml","users.raml","addressTypes.raml","groups.raml"]
      - think: 1
      - log: "Starting test for /_/ramls?path=users.raml endpoint"
      - get:
          url: "/_/ramls?path=users.raml"
          headers:
            X-Okapi-Module-Id: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "application/json"
            - hasProperty: "application/raml+yaml"
      - think: 1
