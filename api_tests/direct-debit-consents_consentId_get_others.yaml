# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /direct-debit-consents/{consentId}_get for http method type GET in artillery framework
# RoostTestHash=20ca2e2ab9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "direct-debit-consents_consentId_get_others.csv"
    fields:
      - "consentId"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        auth: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "Sending incorrect API request - missing authorization header"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending incorrect API request - invalid consentId"
      - get:
          url: "/direct-debit-consents/invalidId"
          headers:
            Authorization: "Bearer {{ auth }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending incorrect API request - invalid URL"
      - get:
          url: "/invalid-url/{{ consentId }}"
          headers:
            Authorization: "Bearer {{ auth }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending correct API request - expecting success"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{ auth }}"
          expect:
            - statusCode: 200
            - hasProperty: "consentId"
      - think: 1
