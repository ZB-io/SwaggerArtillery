# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /direct-debit-consents/{consentId}_get for http method type GET in artillery framework
# RoostTestHash=a281b59bc5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "direct-debit-consents_consentId_get_others.csv"
    fields: ["consentId"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
      vars:
        consentId: "invalidConsentId"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending GET request with incorrect consentId"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "2021-10-01T00:00:00Z"
            x-fapi-customer-ip-address: "192.168.1.1"
            x-fapi-interaction-id: "a1b2c3d4e5f6"
            Accept-Language: "en-US"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
      - think: 1
      - log: "Sending GET request with missing headers"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 401
          - hasProperty: "error"
      - think: 1
      - log: "Sending GET request with incorrect url"
      - get:
          url: "/invalid-url/{{ consentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "2021-10-01T00:00:00Z"
            x-fapi-customer-ip-address: "192.168.1.1"
            x-fapi-interaction-id: "a1b2c3d4e5f6"
            Accept-Language: "en-US"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
      - think: 1
