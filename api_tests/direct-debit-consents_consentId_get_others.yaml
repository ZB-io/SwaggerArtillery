# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /direct-debit-consents/{consentId}_get for http method type GET in artillery framework
# RoostTestHash=20ca2e2ab9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "direct-debit-consents_consentId_get_others.csv"
    fields: ["consentId"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10

scenarios:
  - name: "Scenario for Non-Success API Responses"
    flow:
      - log: "Starting request with invalid consentId"
      - get:
           url: "/direct-debit-consents/{{ consentId }}"
           headers:
             Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
             x-fapi-auth-date: "{{$isoTimestamp}}"
             x-fapi-customer-ip-address: "127.0.0.1"
             x-fapi-interaction-id: "{{$uuid}}"
             Accept-Language: "en-US"
           expect:
             - statusCode: 404
      - think: 1
      - log: "Starting request with missing authorization header"
      - get:
           url: "/direct-debit-consents/{{ consentId }}"
           headers:
             x-fapi-auth-date: "{{$isoTimestamp}}"
             x-fapi-customer-ip-address: "127.0.0.1"
             x-fapi-interaction-id: "{{$uuid}}"
             Accept-Language: "en-US"
           expect:
             - statusCode: 401
      - think: 1
      - log: "Starting request with incorrect URL"
      - get:
           url: "/incorrect-url/{{ consentId }}"
           headers:
             Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
             x-fapi-auth-date: "{{$isoTimestamp}}"
             x-fapi-customer-ip-address: "127.0.0.1"
             x-fapi-interaction-id: "{{$uuid}}"
             Accept-Language: "en-US"
           expect:
             - statusCode: 404
      - think: 1
