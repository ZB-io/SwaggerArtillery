# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /direct-debit-consents/{consentId}_get for http method type GET in artillery framework
# RoostTestHash=20ca2e2ab9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "direct-debit-consents_consentId_get_success.csv"
    fields: ["consentId"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10

scenarios:
  - name: "Test successful API responses"
    flow:
      - log: "Starting test for GET /direct-debit-consents/{consentId}"
      - get:
          url: "/direct-debit-consents/{{ consentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "{{$processEnvironment.x_fapi_auth_date}}"
            x-fapi-customer-ip-address: "{{$processEnvironment.x_fapi_customer_ip_address}}"
            x-fapi-interaction-id: "{{$processEnvironment.x_fapi_interaction_id}}"
            Accept-Language: "{{$processEnvironment.Accept_Language}}"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - hasProperty: "data.consentId"
            - hasProperty: "data.creationDate"
            - hasProperty: "data.status"
            - hasProperty: "data.statusUpdateDate"
            - hasProperty: "data.permissions"
            - hasProperty: "data.permissions.0"
            - hasProperty: "data.expirationDate"
            - hasProperty: "links"
            - hasProperty: "links.self"
            - hasProperty: "links.prev"
            - hasProperty: "links.next"
            - hasHeader: "x-fapi-interaction-id"
      - think: 1
