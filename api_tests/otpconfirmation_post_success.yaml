# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /otpconfirmation_post for http method type POST in artillery framework
# RoostTestHash=3bab7c72fd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "otpconfirmation_post_success.csv"
    skipHeader: true
    fields:
      - "mandateIdentification"
      - "otpIdentificationNumber"
      - "otpPassword"
      - "bankCode"
      - "accountIdentification"
      - "currency"
      - "accountSchemeName"
      - "accountSubType"
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      x-fapi-customer-ip-address: "test_ip"
      x-fapi-auth-date: "test_date"
      x-fapi-interaction-id: "test_id"
      x-idempotency-key: "test_key"

scenarios:
  - name: "Test OTP Confirmation"
    flow:
      - log: "Testing OTP Confirmation API"
      - post:
          url: "/otpconfirmation"
          json:
            mandateIdentification: "{{ mandateIdentification }}"
            otpIdentificationNumber: "{{ otpIdentificationNumber }}"
            otpPassword: "{{ otpPassword }}"
            creditorAccount:
              bankCode: "{{ bankCode }}"
              accountIdentification: "{{ accountIdentification }}"
              currency: "{{ currency }}"
              accountSchemeName: "{{ accountSchemeName }}"
              accountSubType: "{{ accountSubType }}"
          expect:
            - statusCode: 201
            - hasProperty: "processResult"
            - hasProperty: "processResult.responseCode"
            - hasProperty: "processResult.rejectReasonList"
            - hasProperty: "processResult.rejectReasonList.0"
            - hasProperty: "mandateIdentification"
            - hasProperty: "creditorReference"
      - think: 1
