# ********RoostGPT********

# Test generated by RoostGPT for test artillery-UI-insights-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /setup_post for http method type POST in artillery framework
# RoostTestHash=606a0438dd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "setup_post_others.csv"
    fields: ["merchantRequestIdentification", "creditorReference", "ultimateDebtorName", "debtorName", "debtorAccount", "creditorName", "creditorAccount", "debtorPrivateIdentification", "debtorPrivateIdentificationSchemeName", "debtorMobileNumber", "maximumAmountCurrency", "maximumAmount", "occurrences", "otpHoldIndicator", "smsLanguageCode"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
scenarios:
  - name: "Non-Success API Requests"
    flow:
      - log: "Sending invalid payload"
      - post:
          url: "/setup"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-customer-ip-address: "Invalid IP"
            x-fapi-auth-date: "Invalid Date"
            x-fapi-interaction-id: "Invalid Interaction ID"
            x-idempotency-key: "Invalid Idempotency Key"
          json:
            merchantRequestIdentification: "string"
            creditorReference: "string"
            ultimateDebtorName: "string"
            debtorName: "string"
            debtorAccount: 
              bankCode: "string"
              accountIdentification: "string"
              currency: "string"
              accountSchemeName: "string"
              accountSubType: "string"
            creditorName: "string"
            creditorAccount: 
              bankCode: "string"
              accountIdentification: "string"
              currency: "string"
              accountSchemeName: "string"
              accountSubType: "string"
            debtorPrivateIdentification: "string"
            debtorPrivateIdentificationSchemeName: "string"
            debtorMobileNumber: "string"
            maximumAmountCurrency: "string"
            maximumAmount: "string"
            occurrences: 
              frequencyType: "string"
              durationFromDate: "Invalid Date"
              durationToDate: "Invalid Date"
            otpHoldIndicator: "boolean"
            smsLanguageCode: "string"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
      - think: 1
      - log: "Sending unauthorized request"
      - post:
          url: "/setup"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Sending request to non-existing endpoint"
      - post:
          url: "/nonExistingEndpoint"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
