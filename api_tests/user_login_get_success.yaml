# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=474b7881ac
# 
# 

# ********RoostGPT********
config:
  target: "https://api.example.com"  # replace with actual API host
  plugins:
    expect: {}
  payload:
    path: "./path/to/user_login_get_success.csv"  # replace with actual path to CSV
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: User Login Test Scenario
    flow:
      - post:
          url: "/v2/user/login"
          headers:
            api_key: "example_api_key"  # replace with actual API key
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            - hasHeader: "X-Rate-Limit"
            - hasHeader: "X-Expires-After"
            - contentType: "application/json"
      - think: 1
