# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=474b7881ac
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_login_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: User Login Test Scenario
    flow:
      - post:
          url: "/v2/user/login"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
            content-type: "application/x-www-form-urlencoded"
          beforeRequest:
            - "setLoginCredentials"
          expect:
            - statusCode: 200
            - hasHeader: "X-Rate-Limit"
            - hasHeader: "X-Expires-After"
            - contentType: "application/json"
      - think: 1

processors:
  setLoginCredentials: (requestParams, context, ee, next) => {
    requestParams.body = `username=${context.vars.username}&password=${context.vars.password}`;
    return next();
  }
