# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/{username}_get for http method type GET in artillery framework
# RoostTestHash=da281fe471
# 
# 

# ********RoostGPT********
# Artillery YAML Test Script for Non-Success API Responses

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "user_username_get_others.csv"
    fields:
      - "username"
  plugins:
    expect: {}

scenarios:
  - flow:
      # Invalid API request with wrong URL
      - get:
          url: "/v3/user/{{ username }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      # Invalid API request with wrong method type
      - post:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
      - think: 1

      # Invalid API request with missing authorization token
      - get:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: ""
          expect:
            - statusCode: 401
      - think: 1

      # Invalid API request with non-existing user
      - get:
          url: "/v2/user/nonExistingUser"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
