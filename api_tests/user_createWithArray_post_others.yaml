# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/createWithArray_post for http method type POST in artillery framework
# RoostTestHash=b50615cd0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "user_createWithArray_post_others.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses Test Scenario"
    flow:
      - log: "Starting the test with Invalid URL"
      - post:
          url: "/v2/user/invalidUrl"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing with Missing Required Fields in the Payload"
      - post:
          url: "/v2/user/createWithArray"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            firstName: "{{$firstName}}"
            lastName: "{{$lastName}}"
            email: "{{$email}}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing with Invalid API Key"
      - post:
          url: "/v2/user/createWithArray"
          headers:
            api_key: "invalidToken"
          json:
            id: "{{$id}}"
            username: "{{$username}}"
            firstName: "{{$firstName}}"
            lastName: "{{$lastName}}"
            email: "{{$email}}"
            password: "{{$password}}"
            phone: "{{$phone}}"
            userStatus: "{{$userStatus}}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing with Invalid Data Type in the Payload"
      - post:
          url: "/v2/user/createWithArray"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{$id}}"
            username: "{{$username}}"
            firstName: 123
            lastName: "{{$lastName}}"
            email: "{{$email}}"
            password: "{{$password}}"
            phone: "{{$phone}}"
            userStatus: "{{$userStatus}}"
          expect:
            - statusCode: 400
      - think: 1
