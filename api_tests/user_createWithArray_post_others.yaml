# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/createWithArray_post for http method type POST in artillery framework
# RoostTestHash=b50615cd0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "user_createWithArray_post_others.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses Test Scenario"
    flow:
      - log: "Incorrect API usage with Invalid Payload"
      - post:
          url: "/v2/user/createWithArray"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Incorrect API usage with Invalid URL"
      - get:
          url: "/v2/user/invalidURL"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Incorrect API usage with Invalid Authentication"
      - post:
          url: "/v2/user/createWithArray"
          headers:
            api_key: "InvalidToken"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 401
      - think: 1
