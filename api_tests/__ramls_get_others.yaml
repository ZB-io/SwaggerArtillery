# ********RoostGPT********

# Test generated by RoostGPT for test raml-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/ramls_get for http method type GET in artillery framework
# RoostTestHash=347ea070d6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "__ramls_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "GET request to an invalid URL"
      - get:
          url: "/invalid_url"
          expect:
            - statusCode: 404
      - think: 1

      - log: "GET request without required authorization header"
      - get:
          url: "/_/ramls"
          headers:
            X-Okapi-Module-Id: "invalid_module"
          expect:
            - statusCode: 401
      - think: 1

      - log: "GET request with invalid query parameter"
      - get:
          url: "/_/ramls?path=invalid_path"
          headers:
            X-Okapi-Module-Id: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "GET request with missing query parameter"
      - get:
          url: "/_/ramls"
          headers:
            X-Okapi-Module-Id: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
      - think: 1
