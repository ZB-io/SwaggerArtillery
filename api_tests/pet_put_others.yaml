# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet_put for http method type PUT in artillery framework
# RoostTestHash=04bbbce6b4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_put_others.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "status"
    skipHeader: true
  plugins:
    expect: {}
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10

scenarios:
  - name: "Testing non-success API responses"
    flow:
      # Updating pet with invalid auth token
      - put:
          url: "/v2/pet"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1

      # Updating pet with missing required fields
      - put:
          url: "/v2/pet"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      # Updating pet with invalid URL
      - put:
          url: "/v2/invalid_url"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1

      # Updating pet with invalid payload
      - put:
          url: "/v2/pet"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "invalid_url"
            status: "{{ status }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1
