# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet_put for http method type PUT in artillery framework
# RoostTestHash=04bbbce6b4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_put_others.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Invalid PUT request with missing required fields in the payload"
      - put:
          url: "/v2/pet"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            photoUrls: "{{ photoUrls }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Invalid PUT request with incorrect payload format"
      - put:
          url: "/v2/pet"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: 1234
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 415
            - hasProperty: "message"
      - think: 1

      - log: "Invalid PUT request with incorrect URL"
      - put:
          url: "/v2/pet/invalid"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1

      - log: "Invalid PUT request with incorrect authorization token"
      - put:
          url: "/v2/pet"
          headers:
            Content-Type: "application/json"
            api_key: "invalid_token"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1
