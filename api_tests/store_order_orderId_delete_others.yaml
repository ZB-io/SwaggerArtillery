# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework
# RoostTestHash=6e4c4bbafe
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_order_orderId_delete_others.csv"
    fields:
      - "orderId"
    skipHeader: true
  environments:
    staging:
      target: "http://staging.api.target"
      phases:
        - duration: 60
          arrivalRate: 20
          name: "Warm up the application"
        - duration: 120
          arrivalRate: 50
          name: "Sustained max load"
scenarios:
  - name: "Delete purchase order by ID"
    flow:
      - log: "Starting scenario for deleting purchase order"
      - post:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
            - equals: 
                message: "Invalid input"
      - think: 1
      - get:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                message: "Order not found"
      - think: 1
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
                message: "Invalid ID supplied"
      - think: 1
      - delete:
          url: "/v2/store/order/9999"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                message: "Order not found"
      - think: 1
      - log: "Ending scenario for deleting purchase order"
