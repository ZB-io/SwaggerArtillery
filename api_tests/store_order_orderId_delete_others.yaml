# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework
# RoostTestHash=6e4c4bbafe
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "store_order_orderId_delete_others.csv"
    fields:
      - "orderId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "API Error Scenarios"
    flow:
      - get:
          url: "/invalidURL"
          expect:
            - statusCode: 404
      - think: 1

      - delete:
          url: "/v2/store/order/{{ orderId }}"
          expect:
            - statusCode: 401
      - think: 1

      - delete:
          url: "/v2/store/order/invalidOrderId"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - delete:
          url: "/v2/store/order/1001"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - delete:
          url: "/v2/store/order/nonIntegerOrderId"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
