# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_get for http method type GET in artillery framework
# RoostTestHash=4dfd26d5a8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_get_others.csv"
    fields:
      - "petId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      # Testing with an invalid petId
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          think: 1

      # Testing with a missing authorization token
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: ""
          expect:
            - statusCode: 401
            - hasProperty: "message"
          think: 1

      # Testing with an incorrect URL
      - get:
          url: "/v2/pets/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          think: 1
