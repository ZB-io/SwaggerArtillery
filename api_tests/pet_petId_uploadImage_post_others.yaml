# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework
# RoostTestHash=354b987cbe
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_uploadImage_post_others.csv"
    fields: 
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
  environments:
    variable: 
      headers:
        api_key: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Content-Type: "multipart/form-data"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 400
      - think: 1
      - post:
          # Testing an invalid URL
          url: "/v2/invalidURL/{{ petId }}/uploadImage"
          headers:
            Content-Type: "multipart/form-data"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404
      - think: 1
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Content-Type: "multipart/form-data"
            # Omitting the auth token to induce an unauthorized error
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 401
      - think: 1
