# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=474b7881ac
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "user_login_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      - get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Invalid username or password"]
      - think: 2
      - get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          expect:
            - statusCode: 403
            - hasProperty: "message"
            - equals: ["message", "No authentication token provided"]
      - think: 2
      - get:
          url: "/v2/user/logi?username={{ username }}&password={{ password }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Incorrect URL"]
      - think: 2
