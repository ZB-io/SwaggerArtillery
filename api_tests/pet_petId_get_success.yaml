# ********RoostGPT********

# Test generated by RoostGPT for test swagger-artillery-test-ZBIO-5195A using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_get for http method type GET in artillery framework
# RoostTestHash=4dfd26d5a8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_success.csv"
    fields:
      - "petId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Test for successful responses on pet API"
    flow:
      - log: "Fetch details of a pet by petId"
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
      - think: 1
