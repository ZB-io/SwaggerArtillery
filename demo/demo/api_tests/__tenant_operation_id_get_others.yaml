# ********RoostGPT********

# Test generated by RoostGPT for test raml-swagger-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/tenant/{operation_id}_get for http method type GET in artillery framework
# RoostTestHash=139c0a46b1
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "__tenant_operation_id_get_others.csv"
    fields:
      - "operation_id"
    skipHeader: true
    delimiter: "^|^"
    
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Making a GET request with invalid operation_id"
      - get:
          url: "/_/tenant/{{ operation_id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Making a GET request with unauthorized access"
      - get:
          url: "/_/tenant/{{ operation_id }}"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Making a GET request with invalid URL"
      - get:
          url: "/_/invalid/{{ operation_id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Making a GET request with missing operation_id"
      - get:
          url: "/_/tenant/"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
